var app = app || {};

(function(Backbone, _, $) {

  'use strict';

  app.MapView = Backbone.View.extend({

    el: '#map',

    events: {

    },

    initialize: function(options) {
      
    }

  });

})(Backbone, _, jQuery);
